//1.Create an array of 5 colors.
let colors = ['orange', 'red', 'blue', 'purple', 'white', 'black'];

//2.Use a `for` loop to print each color.
for (let i = 0; i < colors.length; i ++){
    console.log(colors[i]);
};